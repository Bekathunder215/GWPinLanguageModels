name: slm-sustainability
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.11
  - pip

  # Core
  - numpy
  - pandas
  - matplotlib
  - tqdm

  # PyTorch (GPU — CUDA 11.8, compatible with DTU HPC V100)
  - pytorch
  - torchvision
  - torchaudio
  - pytorch-cuda=11.8

  # Notebooks (optional but useful)
  - ipykernel
  - jupyterlab

  # For downloading tiny shakespeare in prepare.py (if you use urllib you can remove requests)
  - requests

  - pip:
      - codecarbon
